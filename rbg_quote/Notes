Remember:
The variable csv will be used to hold any data passed by the CSV.parse() method. Each element in a CSV file will be put into an array.
Yahoo!'s stock files contain the same fields, so there is no guesswork involved in what the output should be.
